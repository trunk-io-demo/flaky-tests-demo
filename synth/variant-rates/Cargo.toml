[package]
name = "variant-rates"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "One failure rate per variant, so \"only flaky on macOS\" costs no macOS minutes."

[[bin]]
name = "variant-rates"
path = "src/main.rs"

[dependencies]
junit-gen = { path = "../junit-gen" }

anyhow.workspace = true
clap.workspace = true
