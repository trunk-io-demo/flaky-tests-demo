[package]
name = "branch-rates"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "One failure rate per branch shape, so branch-filtered monitor configuration is demonstrable."

[[bin]]
name = "branch-rates"
path = "src/main.rs"

[dependencies]
junit-gen = { path = "../junit-gen" }

anyhow.workspace = true
chrono.workspace = true
clap.workspace = true
