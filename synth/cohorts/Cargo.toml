[package]
name = "cohorts"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Dated test cohorts whose retirement date is derivable from the test's own name."

[[bin]]
name = "cohorts"
path = "src/main.rs"

[dependencies]
junit-gen = { path = "../junit-gen" }

anyhow.workspace = true
chrono.workspace = true
clap.workspace = true
