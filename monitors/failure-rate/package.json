{
  "name": "@flaky-tests-demo/monitor-failure-rate",
  "version": "0.0.0",
  "private": true,
  "description": "A test that fails a configured percentage of the time, and nothing else.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "@flaky-tests-demo/monitors-utils": "workspace:*"
  }
}
